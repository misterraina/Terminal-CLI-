// import My_placeholder from "./constants.js"

// const root = document.getElementById("app")
// const div_main = document.createElement("div")
// div_main.className = "main_div"

// root.appendChild(div_main)

// const textarea = document.createElement("textarea")

// textarea.id = 'id'
// textarea.name = ''
// textarea.rows = 35
// textarea.cols = 100
// textarea.value = My_placeholder


// textarea.focus()
// textarea.setSelectionRange(textarea.value.length, textarea.value.length)

// textarea.addEventListener('input', (event) => {
//             if (textarea.value.indexOf(My_placeholder) !== 0) {
//                 textarea.value = My_placeholder + textarea.value.slice(My_placeholder.length);
//                 textarea.setSelectionRange(textarea.value.length, textarea.value.length);
//             }
//         });

// div_main.appendChild(textarea)







